[project]
name = "databricks-mcp-server"
version = "0.1.0"
description = "MCP Server exposing Databricks SQL (sandbox)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "fastmcp>=0.3.0",
]

[tool.uv]
dev-dependencies = []


