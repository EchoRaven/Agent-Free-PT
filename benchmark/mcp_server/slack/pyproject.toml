[project]
name = "slack-mcp-server"
version = "0.1.0"
description = "MCP Server exposing Slack Sandbox tools"
requires-python = ">=3.10"
dependencies = [
  "fastmcp>=0.3.0",
  "httpx>=0.27.0",
]

[tool.uv]
dev-dependencies = []


