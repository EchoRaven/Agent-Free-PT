[project]
name = "snowflake-mcp-server"
version = "0.1.0"
description = "MCP Server exposing Snowflake SQL capabilities"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "fastmcp>=0.3.0",
]

[tool.uv]
dev-dependencies = []


